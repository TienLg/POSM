using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using POSManagement.Models;


namespace UnitTest
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void TestMethod1()
        {
            //POSMContext db = new POSMContext();
            //SaleOrder so = db.SaleOrder.Create();
            //so.CustomerId = "002";
            //so.UserId = "003";
            //so.Status = "On progress";
            //so.TakenMoney = 0;
            //so.TotalPrice = 0;
            //so.ShipDate = DateTime.Now;

            //// Save changes
            //db.SaleOrder.Add(so);
            ////db.SaveChanges();

        }
    }
}
